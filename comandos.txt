python setup.py build
pip install pyinstaller
py -m pip install Pillow
pip install flask                     # web framework
pip install requests                  # para chamadas HTTP (bwip-js)
pip install cx_Freeze
pip install windows-curses
pip install waitress
pip install --no-binary :all: waitress
Caso de erro ao instalar o :all waitress pode ser a versão do python ou regra AD, podes voltar para 
o 3.11 ou rodar este: 
--------
Para Rodar em modo DEBUG, precisa configurar antes, (Ctrl + Shifit + P) e escreva "Open launch.json" 
Desta forma estara apotnando para a pasta correta e tenha o Python instalado no vscode



waitress-serve --listen=10.17.30.2:8000 app:app          #Rodar Servidor em LAN

python app.py  # Roda Localmente, mas precisa das dependencias acima

Download de Certificado caso de erro SLL durante download de dependencias
acesse https://pypi.org/ --> Pegue o certificado --> exporte no C:\certs
Inclua o texto a baixo nas varaiveis de ambiente
PIP_CERT "C:\certs\corp-root.pem"
REQUESTS_CA_BUNDLE "C:\certs\corp-root.pem"
SSL_CERT_FILE "C:\certs\corp-root.pem"

Instalar Dependencia -> pip install pyinstaller
Executar no CMD para gerar um executavel Criptografado ->  pyinstaller ImpressoraApp.spec
 pyinstaller --onefile --name ImpressoraApp app.py

 Limpar cache

 python -m compileall -b .
for /r %i in (__pycache__) do rmdir /s /q "%i"