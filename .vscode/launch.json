{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run App (module: app)",
      "type": "debugpy",
      "request": "launch",
      "module": "app",                    // roda app/__main__.py como pacote
      "cwd": "${workspaceFolder}",        // raiz do repo
      "console": "integratedTerminal",
      "justMyCode": false,
      "env": {
        "FLASK_ENV": "development"
      }
    }
  ]
}